function setup_commercial() {
  let jsn;
  
  // Load previous config if available
  if (localStorage.commercial_json) jsn = JSON.parse(localStorage.commercial_json);
  
  // Setup new config
  if (!jsn) {
    //
  }
}

function play_commercial() {
  //
}

setup();
